<%= form_with(model: adjustment_value, local: true) do |form| %>
  <% if adjustment_value.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adjustment_value.errors.count, "error") %> prohibited this adjustment_value from being saved:</h2>

      <ul>
      <% adjustment_value.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :adjustment_list %>
    <%= form.text_field :adjustment_list %>
  </div>

  <div class="field">
    <%= form.label :value %>
    <%= form.text_field :value %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
