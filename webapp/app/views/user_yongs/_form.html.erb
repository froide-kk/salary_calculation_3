<%= form_with(model: user_yong, local: true) do |form| %>
  <% if user_yong.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_yong.errors.count, "error") %> prohibited this user_yong from being saved:</h2>

      <ul>
      <% user_yong.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
