<%= form_with(model: residence_allowance, local: true) do |form| %>
  <% if residence_allowance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(residence_allowance.errors.count, "error") %> prohibited this residence_allowance from being saved:</h2>

      <ul>
      <% residence_allowance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :residence_type %>
    <%= form.text_field :residence_type %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
