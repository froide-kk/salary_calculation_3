<%= form_with(model: adjustment, local: true) do |form| %>
  <% if adjustment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adjustment.errors.count, "error") %> prohibited this adjustment from being saved:</h2>

      <ul>
      <% adjustment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :社員名 %>
    <%= form.text_field :employee_name %>
  </div>

  <div class="field">
    <%= form.label :評価値形態id %>
    <%= form.collection_select(:adjustment_type_id, @adjustment_types, :id, :name) %>
  </div>

  <div class="field">
    <%= form.label :評価値回数 %>
    <%= form.number_field :count %>
  </div>

  <div class="field">
    <%= form.label :データ作成日 %>
    <%= form.date_select :date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
