<%= form_with(model: user_, local: true) do |form| %>
  <% if user_.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_.errors.count, "error") %> prohibited this user_ from being saved:</h2>

      <ul>
      <% user_.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.number_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
