<%= form_with(model: web_personal, local: true) do |form| %>
  <% if web_personal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_personal.errors.count, "error") %> prohibited this web_personal from being saved:</h2>

      <ul>
      <% web_personal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
