<%= form_with(model: web_login, local: true) do |form| %>
  <% if web_login.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_login.errors.count, "error") %> prohibited this web_login from being saved:</h2>

      <ul>
      <% web_login.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
