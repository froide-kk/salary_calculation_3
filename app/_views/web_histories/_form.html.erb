<%= form_with(model: web_history, local: true) do |form| %>
  <% if web_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(web_history.errors.count, "error") %> prohibited this web_history from being saved:</h2>

      <ul>
      <% web_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
